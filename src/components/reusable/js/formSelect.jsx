/**
 * App-wide dropdown — same UI as admission setup (FormScrollSelect).
 * Compact scroll list, hidden scrollbar, smooth scroll, portal menu.
 */

export { default, normalizeSelectOption } from './formScrollSelect';
